run:
  skip-dirs: []

linters:
  disable-all: true
  enable:
    -	errcheck
    - asciicheck
    -	gosimple
    -	govet
    -	ineffassign
    -	staticcheck
    -	typecheck
    -	unused

linters-settings:
  unused:
    check-exported: false

issues:
  exclude-rules:
    - linters:
        - unused